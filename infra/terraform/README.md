# Terraform

Reference Terraform layouts for deploying the platform.

- `modules/` reusable infrastructure components
- `envs/` dev/staging/prod compositions

This is a reference; adapt to your cloud environment.
