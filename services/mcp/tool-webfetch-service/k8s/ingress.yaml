apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tool-webfetch-service
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: tool-webfetch-service.soulism.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tool-webfetch-service
                port:
                  number: 3004
