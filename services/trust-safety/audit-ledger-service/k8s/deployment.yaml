apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-ledger-service
  labels:
    app: audit-ledger-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-ledger-service
  template:
    metadata:
      labels:
        app: audit-ledger-service
    spec:
      containers:
        - name: audit-ledger-service
          image: ghcr.io/soulism/audit-ledger-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 4003
              protocol: TCP
          envFrom:
            - configMapRef:
                name: audit-ledger-service
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
