# Memory Service (MCP)

Stores session and long-term memory with privacy boundaries.

## Responsibilities
- session memory (TTL)
- user memory (opt-in)
- retrieval (kv + vector)

## Security
- memory writes go through policy gate
- deletion is supported and audited
