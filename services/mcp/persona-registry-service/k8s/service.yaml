apiVersion: v1
kind: Service
metadata:
  name: persona-registry-service
  labels:
    app: persona-registry-service
spec:
  selector:
    app: persona-registry-service
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
