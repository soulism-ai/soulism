# OpenAI App

A minimal example integration surface intended for agent runtimes.

This app should demonstrate:
- connecting to MCP services (stdio or HTTP)
- selecting personas
- running policy-checked tools (`tool:webfetch`, `memory:*`, `filesystem:*`)
- supporting confirm mode for policy decisions
- showing policy traceability and budget metadata
