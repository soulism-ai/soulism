apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: risk-budget-service
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: risk-budget-service.soulism.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: risk-budget-service
                port:
                  number: 4002
