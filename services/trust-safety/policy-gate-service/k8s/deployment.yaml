apiVersion: apps/v1
kind: Deployment
metadata:
  name: policy-gate-service
  labels:
    app: policy-gate-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: policy-gate-service
  template:
    metadata:
      labels:
        app: policy-gate-service
    spec:
      containers:
        - name: policy-gate-service
          image: ghcr.io/soulism/policy-gate-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 4001
              protocol: TCP
          envFrom:
            - configMapRef:
                name: policy-gate-service
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
