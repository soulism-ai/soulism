{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "service.fullname" .) .Values.serviceAccount.name }}
  labels:
{{ include "service.labels" . | indent 4 }}
{{- end }}
