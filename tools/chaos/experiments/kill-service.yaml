apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-policy-gate
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: policy-gate-service
  duration: "30s"
