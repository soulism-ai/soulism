# Chaos Experiments

Chaos tests validate resilience under failure.

Examples:
- kill a service pod
- simulate vector store outage
- simulate policy gate outage (tool servers must fail closed)

Store experiments under `tools/chaos/experiments/`.
