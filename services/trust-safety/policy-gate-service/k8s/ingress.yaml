apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: policy-gate-service
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: policy-gate-service.soulism.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: policy-gate-service
                port:
                  number: 4001
