# Official Core Persona Pack

This pack ships default personas that are safe and broadly useful.

Personas are designed to be layered:
- `base` sets global rules
- roles like `engineer` or `researcher` extend base
- `guardian` acts as a safety overlay

This pack should be signed in release pipelines.
