apiVersion: v1
kind: Service
metadata:
  name: memory-service
  labels:
    app: memory-service
spec:
  selector:
    app: memory-service
  ports:
    - name: http
      protocol: TCP
      port: 3002
      targetPort: 3002
  type: ClusterIP
