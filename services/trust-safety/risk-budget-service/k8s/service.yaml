apiVersion: v1
kind: Service
metadata:
  name: risk-budget-service
  labels:
    app: risk-budget-service
spec:
  selector:
    app: risk-budget-service
  ports:
    - name: http
      protocol: TCP
      port: 4002
      targetPort: 4002
  type: ClusterIP
